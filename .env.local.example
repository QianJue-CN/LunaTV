# LunaTV 环境配置示例
# 复制此文件为 .env.local 并填入实际配置

# 存储类型配置
# 可选值: 'localstorage' | 'redis' | 'upstash' | 'kvrocks'
NEXT_PUBLIC_STORAGE_TYPE=redis

# Redis 配置 (当 STORAGE_TYPE=redis 时使用)
# 格式: redis://[username:password@]host:port[/database]
redis://[username:password@]host:port[/database]

# 管理员配置
USERNAME=admin
PASSWORD=your_admin_password

# 应用配置
NEXT_PUBLIC_SITE_NAME=LunaTV

# 开发环境配置
NODE_ENV=development
