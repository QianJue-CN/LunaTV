# LunaTV 环境配置示例
# 复制此文件为 .env.local 并填入实际配置

# ========== 存储系统配置 ==========

# 存储类型配置
# 可选值: 'upstash' | 'postgres' | 'redis' | 'hybrid'
# - upstash: 使用 Upstash Redis 云服务
# - postgres: 仅使用 PostgreSQL 数据库
# - redis: 仅使用 Redis 缓存
# - hybrid: 混合存储（推荐，PostgreSQL + Redis）
STORAGE_TYPE=hybrid

# PostgreSQL 数据库配置
# 用于持久化数据存储（用户信息、配置、收藏夹等）
DATABASE_URL=postgresql://username:password@host:port/database
# 示例: postgresql://user:pass@localhost:5432/lunatv

# Redis 配置
# 用于缓存和高频数据存储（播放记录、搜索历史等）
REDIS_URL=redis://[username:password@]host:port[/database]
# 示例: redis://:password@localhost:6379
# 示例: redis://username:password@localhost:6379/0

# Upstash Redis 配置（云服务）
# 如果使用 Upstash，需要配置以下两项
UPSTASH_URL=https://your-redis-url.upstash.io
UPSTASH_TOKEN=your_upstash_token

# ========== 应用配置 ==========

# 管理员配置
USERNAME=admin
# LUNATV_USERNAME=admin  # Docker部署推荐使用，避免Windows系统USERNAME环境变量冲突
PASSWORD=your_admin_password  # 密码要求：至少6个字符，包含字母和数字

# 站点配置
NEXT_PUBLIC_SITE_NAME=LunaTV

# 开发环境配置
NODE_ENV=development

# ========== 配置说明 ==========
#
# 存储配置:
# 1. 推荐配置（最佳性能）:
#    STORAGE_TYPE=hybrid
#    DATABASE_URL=postgresql://...
#    REDIS_URL=redis://...
#
# 2. 简单配置（仅数据库）:
#    STORAGE_TYPE=postgres
#    DATABASE_URL=postgresql://...
#
# 3. 云服务配置:
#    STORAGE_TYPE=upstash
#    UPSTASH_URL=https://...
#    UPSTASH_TOKEN=...
#
# 4. 缓存配置（仅Redis）:
#    STORAGE_TYPE=redis
#    REDIS_URL=redis://...
#
# 环境变量冲突解决:
# 如果在Windows系统或Docker环境中遇到站长登录问题，
# 请使用 LUNATV_USERNAME 替代 USERNAME 以避免系统环境变量冲突
#
# 用户注册密码要求:
# - 最少6个字符
# - 必须包含字母和数字
# - 不能与用户名或邮箱完全相同
# - 不能是常见的简单密码（如"123456"、"password"等）
